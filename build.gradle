buildscript{
    repositories{
        mavenCentral()
    }
    dependencies{
        classpath 'commons-net:commons-net:3.5'
    }

}
apply plugin: 'base'
apply plugin: 'dominion.deploy'

project.tasks.assemble.dependsOn("preCompile")

deploy{
    location = "laila"
}